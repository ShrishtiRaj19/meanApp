var express = require('express');
var router = express.Router();
var controller = require("../controller/user.js")

// router.post('/getName',controller.insertUser);
// router.post('/login',controller.login);


module.exports = router;
